from __future__ import annotations


def ensure_schema() -> None:
    """Placeholder for startup schema/migration helpers.

    The project currently uses inline SQLite migrations in app.main.
    This module is introduced to make the migration surface explicit and
    allow future extraction without changing runtime behavior.
    """

    # Intentionally empty for now.
    return
